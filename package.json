{
  "name": "particle-animation",
  "version": "1.0.0",
  "description": "A simple particle animation display.",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "predeploy": "rm -rf dist && parcel build src/index.html --public-url .",
    "deploy": "gh-pages -d dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.11.0",
    "gh-pages": "^6.1.1",
    "sass": "^1.70.0"
  },
  "dependencies": {
    "parcel": "^2.11.0"
  }
}
